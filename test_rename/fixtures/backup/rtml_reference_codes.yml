# Reference codes are not necessary because they are generated by RTML if none is otherwise found;
# also, if reference codes were added here then the WS would be sending "duplicate" transactions,
# which is usually illegal.
